#!/bin/bash

echo "This script is deprecated. Playbooks are rendered directly from the submodule at src/content/playbooks-external/."
echo "No local sync is needed. Update content in the external repo and run: git submodule update --remote"
exit 1
